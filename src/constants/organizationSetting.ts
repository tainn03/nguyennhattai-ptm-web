/**
 * The default minimum length for an order code
 */
export const ORDER_CODE_MIN_LENGTH = 10;

/**
 * The default maximum length for an order code
 */
export const ORDER_CODE_MAX_LENGTH = 16;

/**
 * The default minimum length for an order code prefix
 */
export const CUSTOMER_CODE_PREFIX_MIN_LENGTH = 1;

/**
 * The default maximum length for an order code prefix
 */
export const CUSTOMER_CODE_PREFIX_MAX_LENGTH = 5;

/**
 * The default minimum length for an route code prefix
 */
export const ROUTE_CODE_PREFIX_MIN_LENGTH = 5;

/**
 * The default maximum length for an route code prefix
 */
export const ROUTE_CODE_PREFIX_MAX_LENGTH = 8;

/**
 * The default minimum length for an order code by route code
 */
export const ORDER_CODE_MIN_LENGTH_BY_ROUTE_CODE = 14;
