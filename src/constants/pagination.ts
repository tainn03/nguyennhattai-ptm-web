/**
 * The number of rows per page options for a table.
 */
export const PAGE_SIZE_OPTIONS = [10, 20, 50, 100];

/**
 * The number of rows per page options for orders grid data.
 */
export const PAGE_SIZE_ORDER_GRID_OPTIONS = [12, 24, 60, 120];

/**
 * The number of displayed pages in a pagination component.
 */
export const DISPLAYED_PAGE_COUNT = 5;
